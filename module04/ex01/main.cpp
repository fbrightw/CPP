#include "Animal.hpp"
#include "Cat.hpp"
#include "Dog.hpp"
#include "Brain.hpp"

int main()
{
	// int seize = 31;
	// Animal *Array[seize];

	// for (int i = 0; i < seize; i++)
	// {
	// 	if (i % 2 == 0)
	// 		Array[i] = new Cat();
	// 	else
	// 		Array[i] = new Dog();
	// }

	// for (int i = 0; i < seize; i++)
	// 	delete Array[i];

	
}
